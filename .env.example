# Only read by the local gateway (scripts/proxy.mjs), never by the browser app.
# Any key you leave out here is simply taken from the browser instead.
OPENROUTER_API_KEY=sk-or-v1-replace-me
TRIPO_API_KEY=replace-me
OPENAI_API_KEY=replace-me
RUNWAYML_API_SECRET=replace-me
PROXY_PORT=8787
