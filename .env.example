# Only needed if you run the optional local proxy (scripts/proxy.mjs).
# The browser app does NOT read this file - it keeps keys in localStorage.
OPENROUTER_API_KEY=sk-or-v1-replace-me
PROXY_PORT=8787
